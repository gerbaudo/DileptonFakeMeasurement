#!/bin/bash

#PBS -q atlas
#PBS -l nodes=1:ppn=1

cd $PBS_O_WORKDIR
FakePred --etapt -i ${inp} -o ${out} -s ${sample} --matrix-file ${ROOTCOREDIR}/../SusyMatrixMethod/data/FinalFakeHist_Feb_27.root ${opt}
